<% if resource.errors.any? %>
    <% resource.errors.full_messages.each do |message| %>
      <script>

        new Noty({
          type: 'error',
          layout: 'topRight',
          theme: 'sunset',
          text: '<%= message %>'
        }).show();
      </script>

  <% end %>
<% end %>
